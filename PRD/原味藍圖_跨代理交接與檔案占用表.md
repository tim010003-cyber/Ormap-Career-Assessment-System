# 原味藍圖｜跨代理交接與檔案占用表

> 文件狀態：**已啟用／持續維護**  
> 建立日期：2026-07-20  
> 最近更新：2026-07-22
> 用途：回答「誰現在正在修改哪個檔案、何時交還、誰負責驗收」。  
> 注意：本表記錄的是暫時寫入權，不是永久所有權或最終決策權。

---

## 1. 占用規則

1. 寫入前先讀本表與 `git status`。
2. 工作包必須先登記「進行中」與唯一寫入者，才可修改列入占用範圍的檔案。
3. 路徑必須具體；`整個專案`、`所有 HTML` 等模糊範圍無效。
4. 同一路徑若已有進行中占用，不得開始第二個寫入工作包。
5. 審查者以差異、comment 或交接紀錄提出意見，不在占用期間直接搶寫。
6. 完成、暫停、阻擋或換手時，立即更新狀態、驗證與交還欄位。
7. 表中沒有占用紀錄，不代表可直接修改；仍需有使用者授權與明確工作包。
8. 本表本身同樣遵守單一寫入者：更新前先確認工作區沒有另一代理對本表的未完成變更。

### 狀態值

`待接手`、`進行中`、`待審查`、`阻擋`、`已交還`、`取消`。

---

## 2. 目前檔案占用快照

> 快照時間：2026-07-20。未來開始新工作時必須更新，不得依賴此初始快照推定即時狀態。

| 路徑／範圍 | 能力導向預設角色 | 目前寫入者 | 工作包 | 狀態 | 審查者 | 交還／限制 |
|---|---|---|---|---|---|---|
| `AGENTS.md` | 協作治理維護者 | Codex | DOC-GOV-001 | 已交還 | 使用者／Claude 可審閱 | 本輪僅文件治理 |
| `PRD/原味藍圖_跨代理協作契約_v1_2026-07-20.md` | 協作治理維護者 | Codex | DOC-GOV-001 | 已交還 | 使用者／Claude 可審閱 | 契約仍為提案 |
| `AGENTS.md`、`PRD/原味藍圖_跨代理協作契約_v1_2026-07-20.md`、`PRD/原味藍圖_跨代理交接與檔案占用表.md` | 協作治理維護者 | 無 | GOVERNANCE-ALIGN-009 | 已釋放／實作完成／未提交／文件驗證通過／僅工作區可見 | 使用者決策；Claude 協作審閱 | 已收斂登入驗證、多維狀態、非 main 提交、預覽責任與 P0／P1 發布閘門；未修改程式、Firebase、資料或部署 |
| `CLAUDE.md` 協作段落 | Claude 入口文件＋協作治理 | 無 | RELEASE-PREVIEW-005 | 已交還／待 Claude 接手 | Claude／使用者 | 今日整合、驗收、分批提交與 PR 預覽交接已寫入；未修改既有技術架構章節 |
| `PRD/原味藍圖_已知缺陷與風險登記簿.md` | 共同追蹤；工程問題由工程角色驗證 | Codex | DOC-GOV-001 | 已交還 | Claude／使用者 | Claude 可接手補技術驗證 |
| `PRD/原味藍圖_跨代理交接與檔案占用表.md` | 當次協作記錄維護者 | Codex | DOC-GOV-001 | 已交還 | 使用者／Claude | 新任務開始前先 claim |
| `PRD/原味藍圖_評測與白板介面保護清單_v1_2026-07-20.md` | 評測模組＋產品體驗共同維護 | Claude | INTF-001 | 已交還 | Codex 產品驗收 | v1.1 已補 §9A 技術確認；第 1–8 節數項經核對有誤，衝突以 §9A 為準 |
| `PRD/原味藍圖_權限缺陷修復規劃_SEC-PLAN-001_2026-07-20.md` | 工程與營運負責者 | Claude | SEC-PLAN-001 | 已交還 | 使用者授權／Codex 知悉 | 唯讀規劃；未修改 Rules、索引或環境 |
| `tools/twa.html` | 評測模組＋產品體驗 | 無 | DASHBOARD-HOME-006 | 已交還／待技術與產品驗收 | Claude 技術回歸；使用者產品驗收 | 僅統一對外顯示名稱；題目、計分、草稿、資料、Auth 與承重 hook 未改 |
| `tools/hti.html` | 評測模組＋產品體驗 | 無 | DASHBOARD-HOME-006 | 已交還／待技術與產品驗收 | Claude 技術回歸；使用者產品驗收 | 僅統一對外顯示名稱；題目、計分、草稿、資料、Auth 與承重 hook 未改 |
| `report.html` | 報告模組負責者 | 無 | DASHBOARD-HOME-006 | 已交還／待技術與產品驗收 | Claude 技術；Codex 產品 | 僅統一對外報告名稱；權限、資料渲染與 DOM hook 未改 |
| `timeline.html` | 報告／歷程模組負責者 | 無 | DASHBOARD-HOME-006 | 已交還／待技術與產品驗收 | Claude 技術；Codex 產品 | 僅統一對外名稱；查詢、索引與 DOM hook 未改 |
| `whiteboard.html` | 白板模組負責者 | 無 | — | 已交還／未占用 | Claude 技術；Codex 產品 | 保護 Session、mirror 與角色流程 |
| `firestore.rules` | 工程與營運負責者 | 無 | — | 已交還／未占用 | Claude／安全審查 | 存在 P1 未修復項目；另開工作包 |
| `auth-utils.js`、`counselors.html` 權限語意 | 工程與營運負責者 | 無 | — | 已交還／未占用 | Claude／安全審查 | AUTH-001；另開工作包 |
| `firebase.json`、`.github/workflows/*` | 工程與營運負責者／發布執行者 | 無 | — | 已交還／未占用 | Claude／使用者發布核准 | 合併 main 即觸發 live Hosting |
| `dashboard.html` | 產品與體驗負責者 | 無 | DASHBOARD-HOME-006 | 已交還／待技術與產品驗收 | 使用者產品驗收；Claude 做 Auth／Firestore 技術審查 | 品牌化個人空間雛形完成；沿用既有 Auth 與 assessments，未新增 CRM、敏感個資、金流或資料集合 |
| `admin.html` | 內部管理頁 | 無 | DASHBOARD-HOME-006 | 已交還／待技術驗收 | Claude 技術 | 僅統一使用者可見的評測名稱；查詢、權限與資料代碼未改 |
| `tools/twa-intro.html`、`tools/hti-intro.html`、`data/twa-landing.json`、`data/hti-landing.json`、`terms.html` | 內容與評測顯示名稱 | 無 | DASHBOARD-HOME-006 | 已交還／待內容與技術驗收 | 使用者內容；Claude 評測邊界 | 僅統一 TWA／HTI 對外名稱；JSON 僅改文案欄位，key、結構與評測資料未改 |
| `原味藍圖_品牌與網站架構總綱.md`、`名詞對照表.md`、`PRD/原味藍圖_TWA與HTI架構與文案盤點_v1_2026-07-21.md`、`PRD/原味藍圖_公開網站文案盤點與調整表_v1_2026-07-21.md`、`PRD/原味藍圖_跨代理交接與檔案占用表.md` | 產品與文件治理 | 無 | DASHBOARD-HOME-006 | 已交還／待審閱 | 使用者／Claude | 已記錄 Dashboard 雛形、工具名稱決策、驗證與交接；歷史原文引述未被批次改寫 |
| `index.html`、`brand.css`、`about.html`、`blog.html`、`tools.html`、`courses.html`、`services.html`、`organizations.html`、`原味藍圖_品牌與網站架構總綱.md`、`PRD/原味藍圖_公開網站文案盤點與調整表_v1_2026-07-21.md` | 產品與體驗負責者 | 無 | WEB-SHELL-002 | 已交還／待使用者內容校稿與整站預覽 | 使用者產品驗收；Claude 可做技術審查 | Index 展開體驗、公開站閱讀節奏與文案盤點完成；未碰 Dashboard、Auth、Firestore、評測核心、表單、設定或部署 |
| `mockups/**`、`tools/job-design/**` | 產品與體驗／指定模組 | 無 | — | 已交還／未占用 | 依工作包指定 | 不得被視為正式已上線功能 |
| `tools/job-design/index.html`、`tools/job-design/case.html`、`tools/job-design/settings.html`、`tools/job-design/jobdesign-intro.html`、`tools/job-design/jd-theme.css`、`tools/job-design/README.md`、`tools.html`、`organizations.html`、本占用表 | 產品與體驗／職務設計模組整合 | 無 | JOB-DESIGN-INTEGRATION-010 | 已交還／待使用者產品驗收與 Claude 技術驗收／僅工作區 | 使用者產品驗收；Claude 技術驗收 | 網站接線、onboarding、視覺、響應式與可及性已完成；未修改 `brand.css`、任何 `tools/job-design/*.js`、`server/`、穩定 ID／data-*／name、module script、Auth、Firestore、資料或部署；下載入口依現況維持暫停 |
| `tools/job-design/jd-fields.js`、`tools/job-design/index.html`、本占用表 | 產品與體驗／職務設計建立流程 | 無 | JOB-DESIGN-ACCEPTANCE-011 | 已交還／待使用者產品驗收與 Claude 技術驗收／僅工作區 | 使用者產品驗收；Claude 技術驗收 | 組織名稱、職務暫定名稱、建立文件的人已改為選填；建立日期保留自動帶入與必填。入口明示為「AI 設定 · 模擬整理／已啟用」。未修改欄位 ID、資料形狀、AI 提示詞、Key、代理、Auth、Firestore 或部署 |
| `tools/job-design/jd-ai.js`、`tools/job-design/jd-mock-ai.js`、`tools/job-design/case.html`、`tools/job-design/jd-fields.js`、`tools/job-design/README.md`、`tools/job-design/職務設計App_AI輸出品質與流程評估_v1_2026-07-21.md`、本占用表 | 產品與體驗／職務設計 AI 流程 | 無 | JOB-DESIGN-AI-NONBLOCK-012 | 已交還／評估報告完成／既有實作未提交且未視為本輪建構交付／僅工作區 | 使用者產品決策；後續建構代理技術與安全驗收 | 已比對四份測試輸出與真 AI／Mock／UI／文件程式路徑，完成口語轉精煉、正職偏向、成熟度、結構化面試、長表格與下載內容不一致評估。依使用者指示，發現 Claude 同步補下載後未再追加 `case.html`／`jd-word.js` 修改。禁止讀寫 Key、呼叫付費 API、修改 server、Auth、Firestore 或部署 |
| `tools/job-design/職務設計App_AI機制與嚴苛度盤點_v1_2026-07-21.md`、`tools/job-design/職務設計App_AI輸出品質與流程評估_v1_2026-07-21.md`、`tools/job-design/職務設計App_嚴苛度鬆綁_交接Codex_v1_2026-07-21.md`、本占用表 | 職務設計 AI 文件歸位 | 無 | JOB-DESIGN-AI-DOCS-MOVE-013 | 已交還／移動完成／未提交／僅工作區 | 使用者確認；Claude 後續接手 | 已依使用者指示將三份純 Job Design AI 文件移出 PRD，放入評測工具模組資料夾。未修改程式、下載、Auth、Firestore、Hosting 或部署 |
| `tools/job-design/職務設計App_AI輸出品質與流程評估_v1_2026-07-21.md`、`tools/job-design/README.md`、本占用表 | 職務設計 AI 單一評估入口 | 無 | JOB-DESIGN-AI-REPORT-CONSOLIDATE-014 | 已交還／整合完成／未提交／僅工作區 | 使用者確認；Claude 後續接手 | 已以 AI 輸出品質與流程評估報告作為主要入口，整合舊機制盤點、嚴苛度鬆綁、測試輸出、現況差異與後續建構建議；README 已加入入口。舊文件保留為歷史依據。不修改程式、下載或部署 |
| `organizations.html`、`tools.html`、`tools/job-design/jobdesign-intro.html`、`tools/job-design/index.html`、`tools/job-design/jd-theme.css`、本占用表 | 組織方案定位、公開說明與示範體驗入口 | Codex | JOB-DESIGN-POSITIONING-015 | 已交還：實作完成／未提交／靜態驗證通過／瀏覽器尚未驗證／僅工作區可見 | 使用者產品驗收；Claude 可開始唯讀技術審查 | 對外名稱採「職務設計與甄選」，副標為「顧問引導式組織用人決策工具」；只改公開文案、資訊架構、CTA 與展示邊界。不改 JS、資料形狀、AI、下載、Auth、Firestore、金流或部署 |
| `AGENTS.md`、`PRD/README.md`、`PRD/原味藍圖_TWA與HTI架構與文案盤點_v1_2026-07-21.md`、`PRD/原味藍圖_跨代理交接與檔案占用表.md` | 文件與內容治理 | 無 | DOC-CONTENT-001 | 已交還／待使用者校稿 | 使用者內容校稿；Claude 技術審閱 | 閱讀入口與評測底稿完成；未搬檔、刪檔或修改評測程式 |
| `tools/twa-intro.html`、`tools/hti-intro.html`、`index.html`、`tools.html`、`brand.css`、`原味藍圖_品牌與網站架構總綱.md`、`PRD/原味藍圖_公開網站文案盤點與調整表_v1_2026-07-21.md` | 產品與體驗負責者 | 無 | ASSESSMENT-INTRO-001 | 已交還／待產品與瀏覽器驗收 | 使用者內容／產品驗收；Claude 評測與 Auth 邊界審閱 | 公開介紹與入口接線完成；未修改評測、資料、Auth、Rules 或部署 |
| `PRD/需求池.md`、`PRD/原味藍圖_跨代理交接與檔案占用表.md` | 產品需求與協作記錄 | 無 | BACKLOG-AI-008 | 已交還／待審閱 | 使用者／Claude | 已記錄個人空間自主書寫、AI 最小介入、規則式內容推薦與真 AI 啟用前治理條件；未修改網站、資料、AI API 或 Cloud |
| `PRD/原味藍圖_工作價值觀羅盤課程反推與工具校正報告_v1_2026-07-22.md`、本占用表 | TWA 課程與產品內容分析 | 無 | TWA-COURSE-AUDIT-016 | 已交還／報告完成／未提交／靜態驗證通過／僅工作區可見 | 使用者內容確認；後續 Codex 工程執行 | 已完成課程反推、設計意圖、五項校正事項與可直接貼用的 Codex 任務指令；未修改評測、報告、JSON、Auth、Firestore 或部署 |
| `tools/twa.html`、`data/twa-landing.json`、`tools/twa-intro.html`、`data/SYNC_GUIDE.md`、`PRD/原味藍圖_已知缺陷與風險登記簿.md`、本占用表 | TWA 內容一致性校正 | 無 | TWA-CONTENT-FIX-017 | 已交還／實作完成／未提交／靜態驗證通過／瀏覽器與登入未驗證／僅工作區可見 | 使用者內容驗收；後續發布另行核准 | 僅修正四處文案（8 分點數、現況分數用途、兩處名詞層級）。`BUDGET_MAP`、計分、三態、九宮格、雷達、報告演算法、JSON key 與資料形狀、題目 ID、草稿、Auth、Firestore、Rules 均未改。順帶發現並登記 `OPS-003`（sync 腳本會刪欄位），未修腳本 |
| `G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v1.pptx`、本占用表 | 品牌授課簡報設計與製作 | 無 | COURSE-DECK-TEMPLATE-020 | 已交還／範本完成／未納入 Git／逐頁視覺與靜態驗證通過／Google Drive 可見 | 使用者內容與視覺驗收 | 已建立 13 頁 16:9 可編輯 PowerPoint 版型庫，沿用正式 Logo、品牌色、Noto 字體與首頁地景。未覆寫既有檔案，未修改網站、評測、資料、Auth、Firestore、Rules、Hosting 或部署 |
| `G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v2.pptx`、本占用表 | 品牌授課簡報風格深化 | 無 | COURSE-DECK-TEMPLATE-021 | 已交還／v2 完成／未納入 Git／逐頁視覺、重新匯入與版型忠實度驗證通過／Google Drive 可見 | 使用者內容與視覺驗收 | 已深化「原味、流動、勇敢」：重整情緒節奏、探索路徑、紙感痕跡、停頓與行動轉折；v1 保留，未修改網站、評測、資料、Auth、Firestore、Rules、Hosting 或部署 |
| `G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v3_夜航燈塔.pptx`、本占用表 | 夜航燈塔授課簡報系統 | 無 | COURSE-DECK-TEMPLATE-022 | 已交還／v3 完成／未納入 Git／逐頁視覺、重新匯入與版型忠實度驗證通過／Google Drive 可見 | 使用者內容與視覺驗收 | 已以深藍夜航、淡藍照亮、橘色燈塔建立 13 頁商務／授課雙用範本，強化身體重量、張力、方向與專業感；v1、v2 保留，未修改網站、評測、資料、Auth、Firestore、Rules、Hosting 或部署 |
| `G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v4_柔光燈塔.pptx`、本占用表 | 柔光燈塔授課簡報收斂 | 無 | COURSE-DECK-TEMPLATE-023 | 已交還／v4 完成／未納入 Git／逐頁視覺、重新匯入與版型忠實度驗證通過／Google Drive 可見 | 使用者內容與視覺驗收 | 已以 v3 為來源，降低圖片面積、對比與標題張力，增加內容留白與授課承載空間；v1–v3 保留，未修改網站、評測、資料、Auth、Firestore、Rules、Hosting 或部署 |
| `G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v5_星芒座標.pptx`、本占用表 | 星芒座標授課簡報探索 | 無 | COURSE-DECK-TEMPLATE-024 | 已交還／v5 完成／未納入 Git／逐頁視覺、重新匯入與版型忠實度驗證通過／Google Drive 可見 | 使用者內容與視覺驗收 | 已以正式星形 Logo 發展細顆粒、銳利光點、座標線與方向軸；v1–v4 保留，未修改網站、評測、資料、Auth、Firestore、Rules、Hosting 或部署 |

「已交還／未占用」只表示目前沒有在本表登記的寫入者，不表示已驗證或可發布。

---

## 3. 待接手工作包

| 工作包 | 建議接手角色／代理 | 允許範圍 | 禁止範圍 | 交付 | 狀態 |
|---|---|---|---|---|---|
| INTF-001 評測與白板介面技術確認 | 評測模組＋工程與營運／Claude | 唯讀核對 `tools/twa.html`、`tools/hti.html`、`report.html`、`timeline.html`、`whiteboard.html`；更新介面保護清單 | 不修改評測、Rules、資料或正式環境 | 確認／修正 hook、欄位、事件、回歸案例及安全樣式邊界 | **已完成／待 Codex 產品驗收** |
| SEC-PLAN-001 權限缺陷修復規劃 | 工程與營運／Claude | 唯讀分析 SEC-001～004、AUTH-001、OPS-001 | 未經核准不得修改 Rules、索引或部署 | 分拆工作包、權限矩陣、遷移／回復與驗收計畫 | **已完成／待使用者授權實作** |
| SEC-FIX-B 輸出跳脫（SEC-006） | 工程與營運／Claude | `whiteboard.html:646-711`、`report.html:509`、`timeline.html:217-229` | 不動 Rules、資料或權限邏輯 | 三處改安全插入＋渲染回歸 | 待使用者授權 |
| SEC-FIX-C HTI 草稿綁定 uid（SEC-007） | 評測模組／Claude | `tools/hti.html` localStorage 存取路徑 | 不動 Rules、不改計分 | key 綁 uid＋既有草稿處理＋回歸 | 待使用者授權 |
| ASSESSMENT-TECH-REVIEW-004 | 評測模組＋工程驗收／Claude | 先唯讀 `tools/twa.html`、`tools/hti.html` 與 ASSESSMENT-SHELL-003 差異；若確認為本次回歸，再 claim 最小修正檔 | 不改公開頁設計／文案、評測資料、Auth、Firestore、Rules、報告 schema 或部署；不 blanket-stage 工作區 | TWA／HTI 一般、草稿、loadId、wb／mirror、報告與返回 Dashboard 回歸；結論為可／不可進預覽 | **待 Claude 接手** |
| RELEASE-PREVIEW-005 | 工程驗收＋Git／預覽／Claude | 依 `CLAUDE.md` 白名單分開審查、測試與提交 Codex 網站包及 Claude Job Design 包；推非 main 分支並開 PR | 不提交 `.claude/settings.local.json`；不 blanket-stage；不 merge main；不直接 live 部署 | 技術回歸、兩個獨立提交範圍、PR 與 Firebase preview URL、是否包含 Job Design、可／不可合併建議 | **待 Claude 接手；須先完成 ASSESSMENT-TECH-REVIEW-004** |
| DASHBOARD-TECH-REVIEW-007 | 工程驗收／Claude | 唯讀審查 `dashboard.html` 與 DASHBOARD-HOME-006 名稱差異；以真實登入或核准的預覽環境驗證既有流程 | 未另行 claim 前不得修程式；不改 Rules、Auth helper、assessment schema、金流或個資；不合併 main | 登入導轉、本人報告清單、空／錯誤狀態、查看報告、再次探索、刪除、授權、兩筆 TWA 軌跡、手機／桌機回歸；可／不可進預覽結論 | **待 Claude 接手** |

---

## 4. 新工作占用模板

在「占用紀錄」追加一列，並同步更新第 2 節快照：

```text
工作包：
路徑／範圍：
唯一寫入者：
開始日期時間：
目前狀態：進行中
工作分支／PR（若有）：
審查者：
允許修改：
禁止修改：
保護行為：
預計交還條件：
```

### 換手模板

```text
原寫入者：
新寫入者：
交接日期時間：
目前差異：
已完成：
未完成：
驗證結果：
尚未驗證：
阻擋：
新寫入者可開始的明確條件：
```

---

## 5. 占用紀錄

| 日期 | 工作包 | 路徑 | 寫入者 | 動作 | 狀態／結果 |
|---|---|---|---|---|---|
| 2026-07-20 | DOC-GOV-001 | `AGENTS.md`、協作契約、`CLAUDE.md` 協作段落及三份治理文件 | Codex | 建立／修訂治理文件 | 文件工作完成後交還；介面清單待 Claude 技術審查；未修改網站程式、Rules 或部署 |
| 2026-07-20 | INTF-001 | 讀：`tools/twa.html`、`tools/hti.html`、`whiteboard.html`、`report.html`、`timeline.html`（皆唯讀，未修改）。寫：介面保護清單 §9A、缺陷登記簿、本表 | Claude | 逐檔靜態核對並補技術確認 | 已完成並交還。修正原清單 11 項記載（含 4 項死碼誤列為承重）；補列 60+ 項遺漏承重掛點；新增分類、生命週期、樣式邊界與 12 項回歸案例。**未啟動瀏覽器／Emulator，未連線 Firebase** |
| 2026-07-20 | SEC-PLAN-001 | 寫：`PRD/原味藍圖_權限缺陷修復規劃_SEC-PLAN-001_2026-07-20.md`、缺陷登記簿 | Claude | 唯讀分析並提出修復規劃 | 已完成並交還。**新增 SEC-005（P0）**、SEC-006、SEC-007、OPS-002；SEC-002 升 P0 並與 SEC-005 合併；SEC-001 範圍縮小；補齊 `whiteboard_sessions` schema；拆分 9 個工作包。**未修改 Rules、索引或環境**；5 項待使用者決策 |
| 2026-07-20 | SEC-FIX-B（SEC-006） | 寫：`whiteboard.html`、`report.html`、`timeline.html` | Claude | 輸出跳脫 | 已完成並交還。新增 `escHtml()` 於 whiteboard／timeline（report 沿用既有 201 行版本）；`?t=` 收斂為 TWA／HTI 白名單；空 catch 補 warn；`_pendingSession.counselorName` 刻意保留未跳脫原字串（會寫入 Firestore）。**本機實測**：以 `<img src=x onerror=...>` 經 `?c=` 注入，payload 以純文字呈現、未建立元素、未執行，console 無錯誤。**未動 Rules、權限或資料** |
| 2026-07-20 | SEC-FIX-C（SEC-007） | 寫：`tools/hti.html` | Claude | HTI 草稿綁定 uid | 已完成並交還。草稿 key 改 `hti_progress_v4::{uid}`；還原時機延後至 `onAuthStateChanged`（新增 `window.maybeOfferRestore(uid)`）；`restoreProgress()` 改為只渲染市集；**未綁 uid 的舊草稿一律清除**（無法確認歸屬）。**本機實測**：舊 key 於載入時被移除、他人 uid 草稿未被動到、console 無錯誤。**登入後的還原提示流程尚未實測**（需 Google 登入） |
| 2026-07-20 | CLEAN-A（標注移除） | 寫：`whiteboard.html` | Claude | 依使用者決策刪除畫布標注系統 | 已完成並交還。移除 `#anno-canvas`、canvas 事件、`setTool`／`setColor`／`setWidth`／`clearAnno`／`spawnSticky`／`renderPath`／`redraw`／`hexA` 及相關狀態變數；CSS 移除 `.c-dot`／`.w-btn`／`.tool-sep`／`.sticky-*`／`.m-*` 全系列。**保留 `.tool-btn`**（仍用於 nav 面板收合鈕）。殘留引用檢查通過；本機載入無 console 錯誤 |
| 2026-07-20 | WEB-IA-001 | 寫：`index.html`、`dashboard.html`、本占用表 | Codex | 串接公開 Index 與登入後個人空間 | **部分完成／阻擋**。Index 已完成「公開保留＋主動進入我的空間」修改且 module script 語法通過；Hero 凍結文字仍存在。Dashboard 原已完成回首頁與品牌導覽，但最終核對時偵測到共享工作區切至 `chore/remove-whiteboard`，同一檔案出現較晚的白板移除寫入並取代該導覽修改。為避免把 Codex 變更混入阿高的分支，已停止重新套用。待阿高完成、切回可用分支並明確交還 `dashboard.html` 後續作。**尚未執行瀏覽器與真實 Google 登入驗證** |
| 2026-07-20 | WEB-IA-001（續作） | 寫：`dashboard.html`、本占用表；唯讀核對 `index.html` | Codex | 在 Claude 完成的 `chore/remove-whiteboard` 最新基線上補回 Dashboard 導覽 | **已完成並交還／待使用者驗收**。Dashboard 標題改為「我的空間｜原味藍圖」；Logo 與「品牌首頁」提供兩個返回 Index 的入口；登出按鈕補語意與 focus。靜態確認 Index 公開 Session 分支、Dashboard 連結、兩頁 module script 語法、Hero 凍結文字與白板移除狀態。本機瀏覽器驗證：桌機 1440×900 與手機 390×844 均無水平溢位；手機保留品牌與登入入口，主要 CTA 可見；鍵盤焦點有清楚外框；未登入直接開啟 Dashboard 會導回 Index。僅見既有 Tailwind CDN 的 production 警告。**真實 Google 登入、登入後 Dashboard 與跨頁往返尚未驗證；未提交或發布** |
| 2026-07-21 | WEB-SHELL-001 | 寫：`index.html`、`brand.css`、`about.html`、`blog.html`、`tools.html`、`courses.html`、`services.html`、`organizations.html`、`原味藍圖_品牌與網站架構總綱.md`、本占用表 | Codex | 建立公開網站全站骨架 v1 | **已完成並交還／待使用者瀏覽器驗收**。首頁新增全站入口，並建立品牌介紹、內容中心、工具中心、課程、個人服務與組織方案六頁；共用 `brand.css` 的 `site-*` 樣式，不影響既有評測 selector。Hero 凍結文字保留；六項工具成熟度與可操作狀態依總綱呈現。靜態驗證通過：所有本地 HTML 連結目標存在、每頁單一 H1／main、無重複 ID、CSS 大括號平衡、無敏感／Firebase／Demo 引用、`git diff --check` 通過。**尚未執行新頁面的瀏覽器視覺、鍵盤與真實登入驗證；未提交、推送或部署**。未修改共享工作區既有 `tools/job-design/*` 變更。 |
| 2026-07-21 | WEB-SHELL-002 | 寫：`index.html`、`brand.css`、`about.html`、`blog.html`、`tools.html`、`courses.html`、`services.html`、`organizations.html`、`原味藍圖_品牌與網站架構總綱.md`、`PRD/原味藍圖_公開網站文案盤點與調整表_v1_2026-07-21.md`、本占用表 | Codex | Index 展開體驗、公開站閱讀節奏與文案盤點 | **已完成並交還／待使用者內容校稿與整站預覽**。開場以兩片紙面展開、露出既有 Hero 吸引子圖；同一工作階段只顯示一次，可按鈕或 Esc 進入，焦點會交回 `main`，靜態支援 reduced motion。公開站保留 Noto Sans TC／Noto Serif TC，放寬中文字距、行距、段落間距與閱讀寬度。新增逐頁逐區文案盤點表，Hero 凍結內容完整保留。驗證：本地連結、單一 H1、重複 ID、CSS 結構、敏感／受保護引用與 `git diff --check` 通過；Index 經本機瀏覽器驗證首次顯示、按鈕、Esc、工作階段不重複、焦點交接與 390×844 無水平溢位，只有既有 Tailwind CDN 警告。**其餘六個公開頁面尚未逐頁做瀏覽器視覺驗收；reduced motion 僅靜態確認；未提交、推送或部署**。未修改共享工作區既有 `tools/job-design/*` 變更。 |
| 2026-07-21 | DOC-CONTENT-001 | 寫：`AGENTS.md`、`PRD/README.md`、`PRD/原味藍圖_TWA與HTI架構與文案盤點_v1_2026-07-21.md`、本占用表；唯讀：TWA／HTI 與直接內容來源 | Codex | 整理共同閱讀入口並建立評測架構／文案底稿 | **已完成並交還／待使用者校稿**。新增 PRD 閱讀地圖，將使用者校稿、共同基準、治理、安全、技術與歷史文件分流；建立 TWA／HTI 來源、名稱映射、主要流程、內容切分與未來固定製作流程。未搬移、刪除、改名或合併既有文件。 |
| 2026-07-21 | ASSESSMENT-INTRO-001 | 寫：`tools/twa-intro.html`、`tools/hti-intro.html`、`index.html`、`tools.html`、`brand.css`、網站總綱、公開文案盤點、本占用表；唯讀：TWA／HTI 與直接內容來源 | Codex | 建立兩個公開評測介紹頁與可重複製作流程 | **已完成並交還／待使用者內容及瀏覽器驗收**。首頁與工具中心先導向公開介紹頁，介紹頁再連原評測；TWA／HTI 核心檔與資料來源維持乾淨。靜態驗證：兩頁各一個 H1／main、無重複 ID、所有本地連結存在、CSS 大括號 121／121、`git diff --check` 通過。**未啟動瀏覽器、伺服器、登入或 Firebase；未提交、推送或部署**。Claude 後續需審閱登入後 return-to 與評測 Landing 減量邊界。 |
| 2026-07-21 | ASSESSMENT-TECH-REVIEW-004 | 唯讀：`tools/twa.html`、`tools/hti.html` 及其 diff、`data/*.json`、`firebase.json`、PR 預覽 workflow；寫：本占用表 | Claude | ASSESSMENT-SHELL-003 技術回歸審查 | **已完成並交還／結論：可進預覽但不可合併**。靜態核對確認 diff 僅呈現層：TWA 為純 CSS＋一段新增靜態 section（**零 JS 變更**）；HTI 僅 `window.onload` 於 `restoreProgress()` 後加 `startAssessment()`，其餘為 CSS 與 `lp-hero-title` h1→h2。題目、計分、草稿 key、Auth、Firestore、Rules、報告 schema 均未變動。**本機實測**（`npx serve`，未登入）：TWA 五個 Landing section 經 `getComputedStyle` 確認 `display:none`、精簡入口正常渲染、`#identity-section` 存在、未登入約 750ms 導回 Index；HTI legacy landing `display:none`、載入卡片正常、未登入約 500ms 導回 Index。五支評測 JSON 皆 200 且結構正確；45 個 `lp-*` hook 全數保留。`.need-title` IntersectionObserver 與隱藏 section 無耦合；`brand.css` 未被兩支評測頁載入，故公開站樣式不影響評測。**額外發現（非回歸，屬改善）**：`confirmRestore()` 從未呼叫 `startAssessment()`，在 SHELL-003 之前草稿還原確認後會停留在 Landing；本次改動順帶消除此陷阱。**未測**：需真實 Google 登入的路徑——草稿存取與還原、三階段作答、平手處理、報告產出與儲存、`?loadId=`、完成後返回 Dashboard、wb 廣播與 mirror 訂閱。 |
| 2026-07-21 | RELEASE-PREVIEW-005 | 寫：Codex 白名單 19 檔、`tools/job-design/*` 7 檔、本占用表；建立分支 `release/preview-2026-07-21` | Claude | 今日成果技術驗收、分批提交與 PR 預覽 | **已完成並交還／待使用者決定是否合併**。分兩個獨立提交：①Codex 網站／文件／評測整合包，②Claude Job Design 包。逐檔 stage 並以 `git diff --cached --name-status` 核對，未 blanket-stage；`.claude/settings.local.json` 由全域 gitignore 涵蓋，未進入索引。**公開站實測**：9 頁全部 200、零 console error、每頁單一 H1、圖片皆有 alt、nav／footer 齊備；17 條本地連結零 404（含 privacy／terms／feedback）；390／768／1024／1280 四種寬度均無水平溢位；Index 展開動畫實測可用（sessionStorage 單次、Esc 可關、焦點交回 `#top`、reduced motion 走 0ms 分支）。**Job Design 判定 ready 並納入本次預覽**：三個示範案例皆正常渲染、零 JS 錯誤、無溢位。**Hosting 風險已確認**：`firebase-hosting-pull-request.yml` 註解聲稱排除 `tools/job-design/**`，但 `firebase.json` 的 ignore 清單**並未包含**該路徑，實際以 `firebase.json` 為準，故 Job Design 會出現在預覽站與（若合併）正式站；惟該目錄早於 `12c8eb0` 已納入版控並經使用者決策開放公開存取，本次為更新既有公開 Demo 而非新增曝光。**未執行**：merge、live 部署、真實 Google 登入。 |
| 2026-07-21 | ASSESSMENT-SHELL-003 | 寫：`tools/twa.html`、`tools/hti.html`、TWA／HTI 盤點文件、公開文案盤點、本占用表 | Codex | 移除公開說明與評測 Landing 的視覺重複 | **已完成並交還／待 Claude 技術回歸與使用者產品驗收**。TWA 隱藏重複 Hero／價值／用途／SOP／準備區，加入精簡操作入口；HTI 隱藏原 Landing，資料與草稿初始化成功後自動進第一階段。所有既有 Landing DOM／ID／JSON render 與 handler 保留。靜態驗證：CSS／JS 括號、靜態 ID、承重 hook、HTI 初始化順序、六段 inline script 語法與 `git diff --check` 通過；評測資料、報告、Dashboard、Auth、Firestore、Rules 無本輪差異。**未啟動瀏覽器、未登入、未實際產出報告、未提交或部署**。 |
| 2026-07-21 | ASSESSMENT-TECH-REVIEW-004 | 寫：`CLAUDE.md` 協作段落、本占用表 | Codex | 將 ASSESSMENT-SHELL-003 技術驗收工作包交給 Claude | **已完成交接／待 Claude 接手**。交接列明變更摘要、TWA／HTI 回歸矩陣、授權邊界、最小修正規則、dirty worktree 注意事項與「可／不可進預覽」輸出格式；未修改評測或其他技術章節。 |
| 2026-07-21 | RELEASE-PREVIEW-005 | 寫：`CLAUDE.md` 協作段落、本占用表 | Codex | 彙整今日全部本機變更、Claude 驗收、分批提交與 PR 預覽流程 | **已完成交接／待 Claude 接手**。確認目前分支已推至 `be88a04`，但今日公開站、介紹頁、文件與評測減量仍未提交；列出 Codex 白名單、Claude Job Design 獨立候選清單、禁止提交 `.claude/settings.local.json`、先完成評測回歸、PR preview 後停止、未經使用者核准不得合併 main。另確認 workflow 註解與 `firebase.json` 不一致：Job Design 實際未被 Hosting 排除。 |
| 2026-07-21 | DASHBOARD-HOME-006 | 寫：`dashboard.html`、TWA／HTI 顯示名稱相關 HTML／JSON、品牌總綱、名詞對照與文案盤點、本占用表；唯讀：既有 Auth、Firestore 與保護清單 | Codex | 建立品牌化個人空間雛形並統一兩項工具對外名稱 | **已完成並交還／待 Claude 技術回歸與使用者產品驗收**。Dashboard 改為回家區、我的原味藍圖、探索拼圖、最近報告、報告收藏室、內容與成長出口；只用既有 `assessments` 顯示點亮狀態與報告。個人檔案、性別友善欄位、微型評測、文章推薦、付費解鎖與複測定價皆明示為後續，未新增資料或假購買功能。對外名稱統一為「工作價值羅盤」「何倫興趣與行動風格」；`TWA`／`HTI`、`toolType`、JSON key、題目、計分、草稿、報告 schema、Auth 與 Rules 未改。靜態驗證通過：inline script 解析、CSS 括號、靜態 ID、Dashboard 單一 main／H1、本地連結、JSON parse、舊名掃描、保護 token 計數與 `git diff --check`。**未啟動瀏覽器／伺服器，未真實登入，未提交、推送、合併或部署**；既有 `tools/job-design/*` 與 `.claude/settings.local.json` 變更未修改。 |
| 2026-07-21 | DASHBOARD-TECH-REVIEW-007（含 NAMING-FIX） | 唯讀：`dashboard.html`、`report.html`、`timeline.html`、`admin.html`、`terms.html`、`data/*-landing.json`、`firestore.rules`、`sync_landing_sheet.py`、xlsx 主檔；經使用者授權後寫：`report.html`、本占用表 | Claude | DASHBOARD-HOME-006 技術回歸並補完對外名稱 | **已完成並交還／結論：可進預覽但不可合併**。核對屬實：`data/*.json` 僅改文案欄位、key 與結構未動；`toolType`、`TWA`／`HTI` 字面量、題目、計分、草稿、schema 未變。Dashboard Auth／Firestore 審查通過——查詢為 `where(uid==uid)` 綁本人、未新增集合／個資／性別／金流、`updateDoc`／`deleteDoc` 皆有對應 Rules 授權（42-45、47-50）、與舊版查詢形狀相同故未引入新索引需求、新增 `escHtml()` 且所有 innerHTML 插值皆跳脫、四個狀態容器預設 fail-safe。`counselorAccess === true` 與舊版一致，legacy undefined 落差早列於缺陷登記簿第 135 行，非本次回歸。**本機實測**（未登入）：導轉正常約 900ms、零 JS 錯誤、單一 H1／main、無重複 ID、6 條連結零 404、390／768／1024／1280 無水平溢位、5 支 JSON 皆 200 且 parse 正常。**發現並經使用者授權修正**：`report.html` `renderTwaHero()` 的 H2 仍為舊產品名，與同檔 `document.title` 及 HTI 側不一致，已改為「工作價值羅盤報告」並同步移除 `uppercase`；四處報告標題現已一致。**刻意未改**：`data/twa-landing.json`、`twa-intro.html`、`tools/twa.html` 中的「工作價值觀」為心理學概念用語，非產品名。**風險（P3，未修）**：`data/*-landing.json` 由 xlsx 經 `sync_landing_sheet.py` 全量重建，本次為手改 JSON 但未於 `data/SYNC_GUIDE.md` 補「一次性事項」，下次 sync 可能覆蓋改名；本機無 Python／openpyxl，無法跑 dry-run 確認覆蓋方向。**未測**：需真實 Google 登入的本人報告清單、空狀態、查看報告、`?loadId=` 再次探索、刪除、授權開關、兩筆 TWA 軌跡與登入後實機回歸。 |
| 2026-07-21 | BACKLOG-AI-008 | 寫：`PRD/需求池.md`、本占用表；唯讀：既有需求池與協作狀態 | Codex | 記錄個人空間自主書寫與 AI 最小介入產品原則 | **已完成並交還／待使用者與 Claude 審閱**。需求池已新增使用者自主書寫、AI 不代寫自我定義、評測後明確按需呼叫、內容推薦先走規則式、真 AI 啟用前需有 Gateway／配額／快取／成本治理等原則與待辦。**未修改**網站程式、Dashboard、評測、資料、Auth、Rules、AI API 或 Cloud；未呼叫付費服務。欄位細節、AI 權益／價格、供應商與推薦演算法仍待後續決策。 |
| 2026-07-21 | GOVERNANCE-ALIGN-009 | 寫：`AGENTS.md`、協作契約、本占用表；唯讀：爭議清單、缺陷登記、SYNC 指南與發布設定 | Codex | 依使用者回覆收斂協作暫行規則 | **寫入權已釋放／實作完成／未提交／文件驗證通過／僅工作區可見**。使用者允許指定預覽環境的登入後前臺測試，最低驗收方式為預覽網址；採五維交付狀態；允許非 main 分支保存，預覽工作包須指定執行者；有效 P0 預設阻擋受影響發布，P1 逐項判定。Firebase Emulator 僅記為另立工程工作包方向，未安裝、未修改設定、未建立資料。未修改程式、Firebase、Rules、資料、xlsx、其他工作包或外部環境。 |
| 2026-07-21 | JOB-DESIGN-INTEGRATION-010 | 寫：`tools/job-design/index.html`、`case.html`、`settings.html`、新增 `jobdesign-intro.html`／`jd-theme.css`、`README.md`、`tools.html`、`organizations.html`、本占用表；唯讀：職務設計 JS／server 與全站外殼參考 | Codex | 接手職務設計 App 的網站整合、onboarding 與視覺精修 | **寫入權已釋放／實作完成／未提交／部分瀏覽器驗證通過／僅工作區可見**。已接上公開說明與網站入口，完成三頁網站外殼、雙角色 onboarding、三欄工作區、投影與響應式樣式；實測三示範關鍵路徑、四份預覽、四種欄位狀態、1280／1440／1179／390px、引導者唯讀視窗與鍵盤焦點。靜態檢查、模組語法、連結、ID、CSS token 與 diff whitespace 通過。首頁 console 無錯誤；其餘頁面完整 console 記錄、真 AI、登入、Firestore、下載、跨瀏覽器與正式發布未驗證。未修改 `brand.css`、任何 `.js`、`server/`、Auth、Firestore、Rules、資料或部署；產品驗收：使用者，技術驗收：Claude；本輪不合併 `main`。 |
| 2026-07-21 | JOB-DESIGN-ACCEPTANCE-011 | 寫：`tools/job-design/jd-fields.js`、`index.html`、本占用表；唯讀：`jd-store.js`、`jd-ai.js`、`settings.html`、`server/ai-proxy.mjs` | Codex | 依使用者驗收意見放寬建立案例必填並確認 AI 入口 | **寫入權已釋放／實作完成／未提交／靜態驗證通過／僅工作區可見**。三個名稱欄位改為選填，日期仍自動帶入且必填；既有 `createCase()` 空值備援會產生「未命名職務」，欄位 ID 與資料形狀未變。案例列表入口改明示「AI 設定 · 模擬整理／已啟用」。欄位旗標測試、index module syntax、入口標籤與 diff whitespace 通過。AI 現況仍是本機代理原型，不是正式雲端串接；未讀取或寫入 Key、未啟動代理、未呼叫外部 API，未修改提示詞、server、Auth、Firestore 或部署。 |
| 2026-07-21 | JOB-DESIGN-AI-NONBLOCK-012 | 寫：`tools/job-design/職務設計App_AI輸出品質與流程評估_v1_2026-07-21.md`、`tools/job-design/README.md` AI 路徑說明、本占用表；先前已寫但本輪後段停止追加：`jd-ai.js`、`jd-mock-ai.js`、`jd-fields.js`、`case.html`；唯讀：四份測試 `.doc`、`jd-word.js` | Codex | 盤點 AI 非阻擋狀態並評估口語轉精煉、成熟度、甄選問題與文件可讀性 | **寫入權已釋放／評估報告完成／未提交／靜態與本機 Mock 邏輯驗證通過／僅工作區可見**。確認 M1、M3 小節、M6 可嘗試真 AI；M3 成熟度與 M5 永遠走固定 Mock。測試輸出確認口語片段直接進能力名稱、JD 機械套句、固定四階段、每錨點兩題、七欄長表及總時長／階段時長混用。另確認「資深客服」檔名內容實為課程助教，但根因待雙案例連續下載重現。使用者確認 Claude 同步補下載後，未再修改下載程式。未啟動代理、未呼叫付費 API、未讀秘密、未修改 Auth、Firestore、Rules、Hosting、未部署、未提交。 |
| 2026-07-21 | JOB-DESIGN-AI-DOCS-MOVE-013 | 移動三份職務設計 AI 文件：`PRD/` → `tools/job-design/`；寫：本占用表 | Codex | 將模組專用 AI 文件放回 Job Design 評測工具資料夾 | **寫入權已釋放／移動完成／未提交／路徑驗證通過／僅工作區可見**。三份檔案名稱與內容維持不變；PRD 不再存放這三份文件。未修改任何程式、下載、資料、Auth、Rules、Hosting 或部署。 |
| 2026-07-21 | JOB-DESIGN-AI-REPORT-CONSOLIDATE-014 | 寫：`tools/job-design/職務設計App_AI輸出品質與流程評估_v1_2026-07-21.md`、`tools/job-design/README.md`、本占用表；唯讀：另外兩份 AI 歷史文件 | Codex | 建立 Job Design AI 的單一主要評估入口 | **寫入權已釋放／文件整合完成／未提交／Markdown 與路徑驗證通過／僅工作區可見**。主報告已補入早期三項根因、六類硬阻擋、轉換規則缺失、QA-02 差異、目前已鬆綁項目，以及「未確認缺人假設」與「已確認增員決策」的最新區分。README 已連到主報告；未修改程式或下載功能。 |
| 2026-07-22 | JOB-DESIGN-POSITIONING-015 | 寫：`organizations.html`、`tools.html`、`tools/job-design/jobdesign-intro.html`、`tools/job-design/index.html`、`tools/job-design/jd-theme.css`、本占用表 | Codex | 重整組織方案定位、顧問引導價值、公開案例體驗、聯繫承接與未來權益預覽 | **已交還：實作完成／未提交／靜態驗證通過／瀏覽器尚未驗證／僅工作區可見**。Claude 可開始唯讀技術審查；如需修正，另行 claim 具體檔案後再寫入。 |
| 2026-07-22 | TWA-COURSE-AUDIT-016 | 寫：`PRD/原味藍圖_工作價值觀羅盤課程反推與工具校正報告_v1_2026-07-22.md`、本占用表；唯讀：TWA 題目、操作頁、介紹資料與報告邏輯 | Codex | 依工具反推課程知識骨架、記錄設計意圖、校正問題並產出可貼給 Codex 的後續任務指令 | **已交還／報告完成／未提交／靜態驗證通過／僅工作區可見**。完成課程知識骨架、已確認設計意圖、五項校正事項、工作包拆分、驗收建議及可直接貼用的 Codex 指令。驗證：必要章節與 issue ID 齊全，資料核對為 20 項需求／281 個環境指標，`data/SYNC_GUIDE.md` 存在，`git diff --check` 通過。未修改評測、報告、JSON、Auth、Firestore、Rules、Git、預覽或部署。 |

| 2026-07-22 | TWA-CONTENT-FIX-017 | 寫：`tools/twa.html`、`data/twa-landing.json`、`tools/twa-intro.html`、`data/SYNC_GUIDE.md`、缺陷登記簿、本占用表；唯讀：`report.html`、`sync_landing_sheet.py` | Claude | 執行 TWA-COURSE-AUDIT-016 的工作包 A：內容一致性校正 | **已完成並交還／未提交**。修正四處：①`twa.html:1715` 指標預算「8 分 = 13 點」→「12 點」，與 `BUDGET_MAP` 一致（**改文案不改常數**，依報告 §6.1 指示）。②`twa.html:1698` 現況分數「不影響報告結論」為錯誤陳述（`report.html` 實際用它計算三態、九宮格與落差），改為報告建議文字。③`data/twa-landing.json` 的 `hero.stats[1].hint` 由「含 20 項環境指標」改為「六大價值面向、20 項工作需求」。④同類錯誤另見於 `tools/twa-intro.html:38`，一併校正並補上「數百個具體工作情境指標」。**差異僅 4 行，全為顯示文案**：`BUDGET_MAP` 定義未進 diff、JSON key 與結構未動、`toolType`／題目／計分／草稿／schema／Auth／Rules 零變更。**驗證通過**：`twa-landing.json` parse 正常、`twa.html` 3 段 inline script `node --check` 全過、全站殘留字串掃描為 0、`git diff --check` 通過、`git diff --numstat` 確認 1／1／2 行。**新發現並登記 OPS-003（P2）**：`sync_landing_sheet.py` 為整檔覆寫且無分支可產生 `hero.stats`（3 筆）與 `prepareChecklist.items`（2 筆），跑一次 sync 即會刪除；已於 `SYNC_GUIDE.md` 加警告與前置檢查，**未修腳本**（超出本工作包，且本機無 Python／openpyxl 無法驗證）。**未執行**：瀏覽器、登入、`TWA-STATE-004`（0 分狀態）、`TWA-WEIGHT-005`（權重命名）、sync 實跑、提交、推送、預覽、部署。 |

| 2026-07-22 | TWA-STATE-B-018 | 寫：`PRD/原味藍圖_TWA零分作答狀態唯讀盤點_v1_2026-07-22.md`、本占用表；唯讀：`tools/twa.html`、`report.html`、`dashboard.html`、`timeline.html`、`admin.html` | Claude | 執行校正報告 §7 工作包 B：`TWA-STATE-004` 唯讀盤點 | **已完成並交還／未實作／待使用者決策**。確認全站無任何 `touched`／`answered` 旗標（五檔零命中），且 `twa.html:1269-1270` 於載入時對 20 項強制補 0，**作答與否的資訊在寫入草稿前即遺失**，故舊資料無法回推。已列出 13 處 `> 0` 判定點、三種分數組合在評測／草稿／`assessments`／報告／雷達圖的個別行為、舊資料相容限制、兩個方案（A：新增 `touchedNeeds` 陣列；B：分數改可為 `null`）及 9 項回歸矩陣；**建議採方案 A**。**另發現獨立問題**：`report.html:229` 僅排除 `0/0`，因此「只填現況、理想仍為 0」的未完成項會被判為**盈餘**並套用「擁有很多但其實不在乎」文案，且送出前無完成度檢查；此項不需新欄位，建議優先於 0 分改造單獨修復。**附帶觀察**（未登記缺陷）：`whiteboard.html` 已移除，但 `tools/twa.html` 仍保留寫入 `whiteboard_sessions`／`whiteboard_active` 與 mirror 訂閱的休眠殘留程式碼。**未修改**任何程式、資料、Auth、Firestore、Rules 或部署；未啟動瀏覽器或登入。 |

| 2026-07-22 | TWA-STATE-FIX-019 | 寫：`tools/twa.html`、`report.html`、`PRD/原味藍圖_TWA零分作答狀態唯讀盤點_v1_2026-07-22.md`、本占用表 | Claude | 依使用者決策實作方案 A（`TWA-STATE-004`）並修正三態分類邊界 | **已完成並交還／未部署**。新增 `touchedCurrent`／`touchedIdeal` 狀態物件，於兩支滑桿 handler 記錄「操作過」（含刻意停在 0）；`computeStageFor`／`countCompletedNeeds`／`getNeedStatus`／`assessed-count`／`updateDebrief`／`updateNavList` 改讀 touched。**連帶修正**：`report.html` 的 `classifyNeed()` 原本只排除 `0/0`，導致「只填現況、理想未作答」被判為**盈餘**並套用「擁有很多但其實不在乎」文案；新增選填第三參數 `touched` 後，理想未作答即回傳未評分。**舊資料相容**：新增 `inferTouched()`，草稿、`?loadId=` 與 mirror 於欄位缺席時以 `> 0` 回填；`classifyNeed` 無 touched 參數時走原判定，**既有報告結論零變化**。**刻意未改**：`BUDGET_MAP`、115 分上限、九宮格 0-4／5-7／8-10 區間、`>= 2` 三態門檻、雷達圖（`radarScores` 已寫入 `assessments`，改分母會使新舊紀錄無法比較）、`isIndLocked`／top5 等依分數大小的判定。**驗證**：兩檔 inline script `node --check` 全過；抽出 `report.html` 真實 `classifyNeed` 原始碼跑 10 組案例，舊紀錄五組輸出與改動前逐項相同、新紀錄「只填現況」→未評分、「刻意 0/0」→平衡、「真盈餘」保留；抽出 twa.html 真實四個函式模擬狀態機，確認刻意選 0 可推進 Gated-lite 三階段，且無 touched 的舊草稿遷移後階段／目錄／完成數與改動前一致；`git diff --check` 通過；計分核心關鍵字未出現於 diff。**未執行**：瀏覽器、真實 Google 登入、實際產出報告、Firestore 寫入驗證、部署。 |

| 2026-07-22 | COURSE-DECK-TEMPLATE-020 | 寫：`G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v1.pptx`、本占用表；唯讀：`ormap-logo.png`、`brand.css`、`index.html`、品牌總綱與既有首頁預覽 | Codex | 依網站品牌系統建立固定授課簡報範本 | **已完成並交還／Google Drive 可見／未納入 Git**。建立 13 頁 16:9 可編輯 PowerPoint 版型庫：封面、品牌規則、章節、單一觀點、雙欄圖文、四概念、比較、流程、案例、個人練習、小組討論、課程收束與結尾。沿用正式 Logo、暖紙白／深墨藍／沉靜青綠／暖陶橘／淡霧藍／暖砂、Noto Serif TC／Noto Sans TC，以及 `index.html` 的地景與多路徑視覺。**驗證通過**：13 頁逐頁原尺寸檢視、PPTX 重新匯入渲染、所有元素 bbox 均在 1280×720 畫布內、同名檔不存在且未覆寫；來源與目的檔 SHA-256 一致（`D5A80584BF0670A48981F01DACE4D2E8FA6D202E80AD26B8F8F9018A06D407F9`）。`slides_test.py` 因 Windows 工具把成功渲染程序的非零退出碼視為失敗，未產出正式 overflow 報告；已以 layout bbox 與逐頁渲染替代驗證。未修改網站、評測、資料、Auth、Firestore、Rules、Hosting、Git 或部署。 |
| 2026-07-22 | COURSE-DECK-TEMPLATE-021 | 寫：`G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v2.pptx`、本占用表；唯讀：v1 簡報、`ormap-logo.png`、`brand.css`、`index.html`、品牌總綱與既有首頁預覽 | Codex | 依使用者回饋深化原味藍圖的原味、流動與勇敢風格 | **已完成並交還／Google Drive 可見／未納入 Git**。保留 13 頁可編輯課程版型系統，改以「靠近自己 → 允許繞路 → 看見線索 → 勇敢行動」形成情緒節奏；減少卡片式企業排版與重複頁尾，加入紙感地景、未封閉路徑、停頓提問、原話假設與小行動轉折。專屬紙感地景由影像生成模式製作，提示要求暖紙白、墨藍／青綠／霧藍／暖砂地景、未封閉迴圈、陶橘勇敢筆觸、無文字與無 Logo；僅作氣氛視覺，文字與課程結構維持可編輯。**驗證通過**：13 頁逐頁原尺寸檢視、PPTX 重新匯入渲染 13 頁、所有 bbox 均在 1280×720 畫布內、版型忠實度檢查 0 issue、目的檔原先不存在且未覆寫、來源與目的 SHA-256 一致（`3459A1E3ED8112824306844CEB10C154A0CC28DC058E66A01B49D97862B95CF6`）。v1 保留；未修改網站、評測、資料、Auth、Firestore、Rules、Hosting、Git 提交或部署。 |
| 2026-07-22 | COURSE-DECK-TEMPLATE-022 | 寫：`G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v3_夜航燈塔.pptx`、本占用表；唯讀：v2 簡報、正式 Logo、品牌色與使用者選定的夜航燈塔視覺 | Codex | 將夜航燈塔方向製作成 13 頁商務／授課雙用可編輯範本 | **已完成並交還／Google Drive 可見／未納入 Git**。建立 13 頁 16:9 可編輯 PowerPoint，以深藍承接夜航、方向與決策，以淡藍承接感受、分析與照亮，橘色只標示燈塔與下一步行動；視覺加入重量、張力、光束與前進方向，同時保留商務場合所需的清楚層級。使用者選定的夜航燈塔影像作為封面、章節與結尾氣氛視覺；Logo 與授課文字仍可編輯。**驗證通過**：13 頁逐頁原尺寸檢視、PPTX 重新匯入渲染 13 頁、所有 bbox 均在 1280×720 畫布內、版型忠實度檢查 0 issue、目的檔原先不存在且未覆寫、來源與目的 SHA-256 一致（`3C460CD07C6B50815AAA2A383862D2C7A5EFA5DD29C1111AEF1472235693BF85`）。v1、v2 保留；未修改網站、評測、資料、Auth、Firestore、Rules、Hosting、Git 提交或部署。 |
| 2026-07-22 | COURSE-DECK-TEMPLATE-023 | 寫：`G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v4_柔光燈塔.pptx`、本占用表；唯讀：v3 簡報、正式 Logo、品牌色與既有夜航燈塔視覺 | Codex | 將 v3 收斂為內容優先的柔光燈塔版本 | **已完成並交還／Google Drive 可見／未納入 Git**。保留原有 13 頁、內容、頁序、Logo 與雙基底概念；第 1、3、13 頁的全版圖像改為右側視覺帶，第 5 頁圖像縮為輔助框，深色節奏頁降低藍黑強度，大標題縮小一階，橘色粗線改細，使內容先被閱讀、品牌氣氛第二眼出現。**驗證通過**：13 頁逐頁原尺寸檢視、PPTX 重新匯入渲染 13 頁、所有 bbox 均在 1280×720 畫布內、版型忠實度檢查 0 issue、目的檔原先不存在且未覆寫、來源與目的 SHA-256 一致（`58030836FE53DFCFF9D078ECB475E657AAB83A43FD0528C965A48E873D0FE2FA`）。v1–v3 保留；未修改網站、評測、資料、Auth、Firestore、Rules、Hosting、Git 提交或部署。 |
| 2026-07-23 | COURSE-DECK-TEMPLATE-024 | 寫：`G:\我的雲端硬碟\7.課程設計\簡報模板\原味藍圖_授課簡報範本_v5_星芒座標.pptx`、本占用表；唯讀：v4 簡報、正式 `ormap-logo.png`、品牌色 | Codex | 將正式星形 Logo 發展成顆粒度與銳利感更高的授課簡報系統 | **已完成並交還／Google Drive 可見／未納入 Git**。保留 v4 的 13 頁內容、頁序與內容優先原則，將夜航燈塔換成「星芒座標」：封面、第 3 與第 13 頁使用右側細顆粒星圖與方向座標，第 4、5 頁使用收斂的星圖輔助視覺，第 2、4–12 頁以小型正式星形 Logo 作頁面識別；深藍、淡藍、橘色仍為唯一主色系。正式 Logo 取自專案 `ormap-logo.png`，未由生成影像重畫；生成影像僅作無文字、無 Logo 的抽象星芒背景。**驗證通過**：13 頁逐頁原尺寸與整套縮圖檢視、PPTX 重新匯入渲染 13 頁、所有 bbox 均在 1280×720 畫布內（0 issue）、版型忠實度檢查 0 issue、目的檔原先不存在且未覆寫、來源與目的 SHA-256 一致（`28E98F970A35F14AF77C2A2EB972967069389D54E0EC1597E53FF13928CDADD2`）。v1–v4 保留；未修改網站、評測、資料、Auth、Firestore、Rules、Hosting、Git 提交或部署。 |

---

## 6. 衝突處理

- 發現同檔重疊：雙方都停止新寫入，先保存各自差異摘要，不執行 reset、checkout 或覆寫。
- 由使用者或當次協作記錄維護者決定先後順序與唯一整合者。
- 先完成的一方提交可審查交接；後接者在最新基線上繼續。
- 若變更已進正式環境，以保護資料與恢復服務為優先，但仍需取得緊急修正授權。
- 無法確認表格是否即時時，視為「尚未取得寫入權」，只可唯讀。

---

## 7. 變更紀錄

| 日期 | 變更 |
|---|---|
| 2026-07-20 | 建立初始占用快照、DOC-GOV-001 交還紀錄及 Claude 待接手工作包。 |
| 2026-07-21 | 登記 WEB-SHELL-001 公開網站全站骨架工作包，並將 `dashboard.html` 與新一輪公開網站占用拆開。 |
| 2026-07-21 | 登記 WEB-SHELL-002：Index 展開體驗、公開站閱讀節奏與全站文案盤點。 |
| 2026-07-21 | 登記 DOC-CONTENT-001：PRD 閱讀地圖與 TWA／HTI 架構、文案底稿。 |
| 2026-07-21 | 登記 ASSESSMENT-INTRO-001：TWA／HTI 公開介紹頁、入口接線與固定製作流程。 |
| 2026-07-21 | 登記 ASSESSMENT-SHELL-003：評測內長篇 Landing 減量與精簡開始畫面。 |
| 2026-07-21 | 登記 ASSESSMENT-TECH-REVIEW-004：Claude 技術回歸與進預覽判定。 |
| 2026-07-21 | 登記 RELEASE-PREVIEW-005：今日整合驗收、分批提交與 Firebase PR 預覽。 |
| 2026-07-21 | 登記並完成 DASHBOARD-HOME-006：品牌化個人空間雛形、TWA／HTI 對外名稱統一與 DASHBOARD-TECH-REVIEW-007 交接。 |
| 2026-07-21 | 登記並完成 BACKLOG-AI-008：個人空間自主書寫、AI 最小介入、規則式內容推薦與真 AI 啟用前治理條件。 |
| 2026-07-21 | 登記 GOVERNANCE-ALIGN-009：收斂登入後預覽測試、多維交付狀態、非 main 保存、預覽執行者與 P0／P1 發布閘門。 |
| 2026-07-21 | 完成 JOB-DESIGN-AI-NONBLOCK-012 評估交接：新增 AI 輸出品質與流程評估，釋放寫入權，後續建構待使用者決策與新工作包。 |
| 2026-07-21 | 完成 JOB-DESIGN-AI-DOCS-MOVE-013：三份 Job Design AI 文件由 PRD 移至 `tools/job-design/`，交由 Claude 後續處理。 |
| 2026-07-21 | 完成 JOB-DESIGN-AI-REPORT-CONSOLIDATE-014：AI 輸出品質與流程評估成為模組主要入口，整合歷史盤點與鬆綁內容。 |
| 2026-07-22 | 完成 TWA-COURSE-AUDIT-016：新增工作價值觀羅盤課程反推與工具校正報告，記錄設計意圖、五項校正事項與後續 Codex 任務指令，並釋放寫入權。 |
| 2026-07-22 | 完成 TWA-CONTENT-FIX-017（工作包 A）：校正 `TWA-CONTENT-001`／`002`／`003` 共四處文案；新增缺陷 `OPS-003`。`TWA-STATE-004` 與 `TWA-WEIGHT-005` 仍待使用者決策，未實作。 |
| 2026-07-22 | 完成 COURSE-DECK-TEMPLATE-020：依網站品牌系統建立 13 頁可編輯授課簡報範本，逐頁渲染與畫布邊界驗證通過，寫入使用者指定 Google Drive 資料夾並釋放寫入權。 |

| 2026-07-22 | JOBDESIGN-M1-OPENING-FIX-021 | 寫：`tools/job-design/jd-mock-ai.js`、本占用表；唯讀：`tools/job-design/jd-fields.js`、`jd-ai.js`、`jd-transform-rules.js`、`case.html`、`functions/index.js` | Claude | 修復問題釐清「開場」章 AI 整理回空白（使用者回報「無法成功讓 AI 運作」） | **已完成並交還／未部署／待使用者驗證**。根因：`M1_CHAPTERS` 有六章（開場＋第一～五章），但 `jd-mock-ai.js` 的 `M1_HANDLERS` 只註冊第一～五章，缺「開場」，因此 `runM1Chapter('開場')` 落入 fallback `envelope('M1', {cannot_complete:true})`，連鎖造成：`task_code` 變 `'M1'` 使 `transformRule('M1-00')` 取不到轉換原則、`organized_content` 為 `{}` 使 `jd-ai.js` 的 `shapeHint` 指示 AI 輸出空物件、`case.html:878` 因而零欄位寫入；且 AI 呼叫成功仍照扣配額。**佐證**：Cloud Functions 日誌自 07:46 部署後僅兩次呼叫（`M1-01`、`SYNTH-01`），皆 `jdAi 完成` 無錯誤，從未出現 `M1-00`；線上 `Cache-Control: no-cache` 已生效，排除快取因素。**修法**：`runM1Chapter` 改以 `M1_CHAPTERS` 查表取得章節，`task_code` 一律用 `ch.task`；新增 `chapterShape()` 由 `ch.outputs` 產生完整欄位形狀（`list`→`[]`、`repeat`→單列樣本、其餘→`''`），供無 handler（開場）與空白輸入兩種情形使用；章節碼查無對應時改為 `throw`，不再靜默回空信封。**刻意未改**：第一～五章有內容時仍完全走原 handler，輸出與改動前逐鍵相同。**驗證**：Node ESM 載入實際模組，六章 ×（有內容／空白）共 12 組，`task_code` 全部等於 `ch.task`、`transformRule()` 全部取得轉換原則、複製 `jd-ai.js` 的 `skeleton()` 確認形狀鍵數皆非零；開場由 0 鍵改為 4 鍵（`m1.org_what`／`org_who`／`org_value`／`org_beliefs`）。`git diff --check` 通過。**未執行**：瀏覽器、Google 登入、真實 AI 呼叫、配額扣抵驗證、commit、部署。**附帶發現並於同工作包續修（見下列 021B）**：第一、三、四、五章的規則式 handler 只吐出關鍵字比對到的鍵，缺鍵會隨輸入內容忽有忽無。 |

| 2026-07-22 | JOBDESIGN-M1-SHAPE-021B | 寫：`tools/job-design/jd-mock-ai.js`、`tools/job-design/case.html`、本占用表；唯讀：`jd-fields.js`、`jd-ai.js`、`jd-transform-rules.js`、`jd-store.js` | Claude | 依使用者決策（方案甲）補齊第一～五章送給 AI 的欄位形狀 | **已完成並交還／未部署／待使用者驗證**。**問題**：規則式 handler 只回傳關鍵字比對到的鍵，因此同一格會隨口述內容忽有忽無（例：口述未提期限，`m1.time_sensitivity` 整格從形狀消失），AI 並非判斷不必填，而是不知道有該欄位。實測缺鍵：第一章 `demand_nature`；第三章 `importance`／`time_sensitivity`／`need_scale`；第四章 `minimum_result`／`ideal_result`；第五章 `investment_status`／`status_reason`。**使用者決策（2026-07-22，方案甲）**：口述優先，只要是 AI 轉換得到的欄位就全部讓 AI 填草稿，人再比對或補充；含原標記 `ai: 'H'` 的欄位。**修法**：`runM1Chapter` 於 handler 結果上墊 `chapterShape(ch)`（`{...shape, ...handler}`，handler 值優先），handler 只呼叫一次。**連帶必要防護**：形狀補完整後 AI 會對填不出來的欄位回空字串／空陣列，而 `case.html` 原本無條件 `S.saveField` 覆寫，會**清空使用者手寫內容或上一輪整理結果**；新增 `aiEmpty()` 判斷，空值一律 `continue` 跳過該格，清空只能由人執行。**驗證**：Node ESM 載入實際模組，六章 ×（長／短／空白口述）共 18 組，`task_code` 全對、`transformRule()` 全部取得、欄位形狀與 `jd-fields.js` 定義**完全一致且無多餘鍵**；回歸抽查第一章確認 handler 比對到的 `trigger_event`／`confirmed_facts` 未被空殼覆蓋，新補的 `demand_nature` 為空字串。以真實 `aiEmpty()` 模擬寫入迴圈，確認人手寫欄位保留、AI 有填欄位寫入、AI 空值不建立欄位。`case.html` inline module `node --check` 通過。**未執行**：瀏覽器、Google 登入、真實 AI 呼叫、配額驗證、commit、部署。 |
| 2026-07-22 | 完成 JOBDESIGN-M1-OPENING-FIX-021：修復問題釐清「開場」章因 `M1_HANDLERS` 未註冊而落入 fallback，導致 AI 整理回空白且照扣配額；`runM1Chapter` 改由 `M1_CHAPTERS` 查表取 task_code 與欄位形狀，未知章節改為拋錯。 |
| 2026-07-22 | 完成 JOBDESIGN-M1-SHAPE-021B：依使用者決策（方案甲）讓第一～五章送給 AI 的欄位形狀恆為完整，含原標記 `ai: 'H'` 的欄位；並在 `case.html` 加上空值防護，避免 AI 回空值清掉人手寫的內容。 |
| 2026-07-22 | JOBDESIGN-M1-INFER-022 | 寫：`tools/job-design/case.html`、本占用表；唯讀：`jd-fields.js`、`jd-ai.js`、`jd-transform-rules.js` | Claude | 依使用者決策開啟 M1 的推論與邏輯鏈整合（「輕」力道） | **已完成並交還／待部署**。**問題**：`AI-I`（AI 推論·待確認）在系統裡是一等公民——M1 有 10 個 `AI-I` 欄位、專屬橘色 badge、專屬 `ai_inference` source_type、專屬 `inferences_requiring_confirmation` 陣列——但提示詞把它關掉了三處：①`case.html` 組 instruction 時只送 `id｜name｜type`，**未送 `o.ai`**，AI 不知道哪些格子是給它推論的，只能用「他講過才寫」的標準處理；②`COMMON_RULES`「補**一個**進 inferences…**說一次就好**」把推論硬限縮為約 1 則；③`STANCE`「不要為了填滿而追問或**補寫**」與 `AI-I` 欄位的設計意圖直接矛盾。**使用者決策（2026-07-22）**：採「輕」力道；**明確表示不需嚴格區分 AI-O／AI-I 兩類欄位**；主要訴求是「口述完整但破碎、邏輯混亂，希望 AI 打破、重組、串聯邏輯鏈」，並認為互動過程會補齊內容，不擔心 AI 推論過多。**修法（僅動 ①，且僅在 `case.html`）**：欄位定義改送 `id｜name｜type｜角色`；新增 `AI_ROLE_HINT` 對照表（與畫面 badge 用的 `F.AI_ROLE_LABEL` 分開，讀者不同）；instruction 增加兩句——重組並串起因果前後關係使草稿比原話通順，以及邊界句「串的是他已經講過的事情之間的關係，延伸判斷放進推論欄位或 inferences」。**刻意未改**：`COMMON_RULES`、`STANCE`、`jd-transform-rules.js` 的逐章 `RULES`——該檔檔頭載明「內容以使用者的正本為準，要修改請先回頭改 Drive 上的文件」，故本次全部改動限於程式組出的 instruction，不觸碰可能屬 Drive 正本的文字。**與方案甲的一致性修正**：`H` 欄位若沿用 badge 文案「人工決定」會反過來叫 AI 別碰，牴觸 07-22 方案甲（AI 轉換得到的欄位一律先給草稿）；因此 `AI_ROLE_HINT.H` 寫為「人工決定（你仍先給草稿）」。**驗證**：`case.html` inline module `node --check` 通過；自 `case.html` 原始碼抽出實際 instruction 樣板重建六章文字，確認角色欄正確對應（第三章：3 整理／3 推論／2 人工決定先給草稿）、兩句新指示存在、無樣板字串殘留。`git diff --check` 通過。**未執行**：真實 AI 呼叫、產出品質比對、瀏覽器、登入。**待使用者驗證**：推論欄位是否開始有內容、邏輯鏈是否變通順、是否推論過頭。若仍偏保守，下一段槓桿是 ②③，但屬 Drive 正本權責，需使用者先決定。 |
| 2026-07-22 | 完成 JOBDESIGN-M1-INFER-022：依使用者決策以「輕」力道開啟 M1 推論與邏輯鏈整合，僅改 `case.html` 組出的 instruction，未觸碰可能屬 Drive 正本的 `COMMON_RULES`／`STANCE`／逐章 `RULES`。 |
| 2026-07-23 | JOBDESIGN-M2MIX-UX-024 | 寫：`tools/job-design/case.html`、`jd-fields.js`、`jd-mock-ai.js`、`jd-word.js`、`jd-theme.css`、`codes.html`、`functions/index.js`、本占用表 | Claude | 依使用者 2026-07-22 回饋處理六項：M2 整合決策重設計、M3 空值覆寫、多選移除鍵、Word 格式對齊、配額預設、預期價值產出查證 | **已完成並交還／Hosting 已部署／Functions 未部署**。**① M2 整合決策重設計**（使用者指定流程：口述→AI 整合成量化配置表→人改數字，AI 先填基礎內容）：新增 `m2._raw_mix` 口述、`task: 'M2-05'`、四欄配置表 `m2.resource_plan`（合作形式／人數／負責範圍／預估成本，標記 `AI-I`）、`ENGAGEMENT_FORMS`（與 `ENGAGEMENT_TRADEOFFS` 共用同一份清單）、兩題量化提問；`renderM2Mix()` 提供口述區與 AI 按鈕，`renderM2Block` 支援 `repeat` 型欄位，`runM2Mix()` 由 `M2_PARTS` 反射出欄位形狀（不手抄）；提示詞要求 AI **主動估一個具體起點**（人數帶單位、成本帶金額單位），並要求其餘欄位與表格對得上。刻意**不**在 `jd-transform-rules.js` 新增 `M2-05` 規則——該檔正本在使用者 Drive，故全部指示寫在 `case.html` 組出的 instruction。**② M3 空值覆寫**：`renderSection` 的寫入迴圈補上 `aiEmpty()` 跳過（M1 已於 021B 修，M3 是漏網）。**③ 多選無法移除**：`cellInput` 的 `type='multi'` chip 加上 ✕（`stopPropagation` 避免順手收合 `details`）、面板勾選同步（`syncChecks`）、`summary` 補 chevron 開合指示（`jd-theme.css` 新增 `.jd-multi`，僅用 transform 未新增色票）。註：`repeat` 的列刪除鍵原本就存在，使用者遇到的是 `multi`。**④ Word 與預覽格式對齊**（子代理執行）：根因為 Word 忽略後代與 class 選擇器；於 `wrap()` 前新增 `inlineStyles()` 樣式攤平、`MSO_HEAD` 頁面設定條件註解；順帶修好 `BODY_STYLE` 用雙引號寫字體名會截斷 `style` 屬性的真 bug。**⑤ 配額預設 30→60**：`codes.html` 兩處與 `functions/index.js` 的 fallback。**⑥ 預期價值產出**：查證為 `m3.value_outputs`（M3 §1.1），欄位與 mock 皆存在，判定是②造成的空值覆寫，修②後應恢復；**未經使用者實跑確認**。**驗證**：三個種子案例×四份文件共 12 份產出全數成功；移除所有 `style="…"` 後與改動前逐份比對內容一致；`full_design` 路徑下配置表四欄齊全、兩列印出、缺值顯示待確認、已套 inline 樣式；無資料時不留空殼；多選移除邏輯三案通過；`case.html`／`codes.html` inline module `node --check` 通過；`git diff --check` 通過。**未執行**：真實 AI 呼叫、實際下載後以 Microsoft Word 開啟、瀏覽器、登入、Functions 部署。**已知落差**（子代理誠實列出）：Word 端字型 fallback、`letter-spacing`、寬表欄寬、列印不印背景色、inline 元素邊框等仍可能與預覽不同。 |
| 2026-07-23 | 完成 JOBDESIGN-M2MIX-UX-024：M2 整合決策改為「口述→AI 量化配置表→人改數字」，並修 M3 空值覆寫、多選移除鍵、Word 格式對齊、配額預設 60。 |
| 2026-07-23 | JOBDESIGN-QUOTA-TOPUP-025 | 寫：`functions/lib/quota.js`、`functions/index.js`、`tools/job-design/index.html`、`case.html`、`codes.html`、`jd-ai.js`、本占用表 | Claude | 使用者回報：次數用完後無處補充；授權碼無法刪除 | **已完成／Functions 已部署／Hosting 待部署**。**① 配額加值**：根因為 `redeemCode` 硬性一人一碼（`userSnap.data().code !== code` 直接丟 `failed-precondition`），因此使用者手上有新碼也兌換不了，前端補入口無用。依使用者需求改為**加值**：`quotaTotal = 既有 + 新碼`，`quotaUsed` 保留，`isInstructor` 只會被加上不會被降級，新增 `codes` 陣列（`arrayUnion`）記錄所有兌換過的碼。**防濫用改由碼本身承擔**：`jd_codes.redeemedBy` 只寫入一次，故同一組碼只能加值一次；重按同一組碼為冪等（`added: 0`、`alreadyRedeemed: true`），不加值也不重設用量。**② 前端補充入口**：`index.html` 的 `paintQuota` 抽出 `redeemForm()`，改為**任何狀態都可展開加值**（剩餘 ≤5 次時自動展開），並回報「已加值 N 次」或「先前已用過」；`case.html` 的 `showAiUnavailable` 由僅 `no_code` 擴充到 `exhausted` 也提供輸入框；`jd-ai.js` 的 `exhausted` 文案補上可加值說明。**③ 刪除授權碼**：新增 `deleteCode` callable（限 Super Admin），**僅允許刪除未兌換的碼**；已兌換者回 `failed-precondition` 並引導改用停權。這不只是保護紀錄——碼被刪後同字串可重新產生並再兌換，加值次數會被無限重放，故為加值模型的必要條件。`codes.html` 每列新增「刪除」，已兌換者 disabled 並以 title 說明。**④ 連帶修正誤導文案**：原停權確認寫「對方會立刻無法使用 AI 整理」，與實際不符——`consumeQuota` 只讀 `jd_users`，不回頭檢查碼的 `revoked`，故停權只擋之後的兌換，不收回已發出的次數。已改為據實說明。**驗證**：`functions/index.js`、`lib/quota.js`、三支 HTML 的 inline module 皆 `node --check` 通過；`jd-ai.js` 於 ESM 環境載入成功並確認新文案；以實際計算式模擬五種兌換情境（首次／重按同碼／第二張碼／講師碼／之後學員碼），加值、冪等、用量保留、講師不降級全部符合預期；`firebase deploy --only functions` 成功，`functions:list` 確認 `deleteCode` 已建立、其餘七個更新成功；`git diff --check` 通過。**未執行**：真實登入下的兌換／刪除端到端測試、Hosting 部署（本則撰寫時）。**待使用者決策**：停權是否應真正收回已兌換者的額度（需 `consumeQuota` 反查碼狀態，屬行為變更）；配額是否走向付費點數。 |
| 2026-07-23 | 完成 JOBDESIGN-QUOTA-TOPUP-025：授權碼兌換改為加值（同碼只加一次）、全站補上加值入口、新增未兌換碼的刪除功能，並修正停權的誤導文案。 |
| 2026-07-23 | JOBDESIGN-DOC-FORMAT-025 | 寫：`tools/job-design/jd-word.js`、`tools/job-design/jd-theme.css`、本占用表；唯讀：`jd-seed.js`、`jd-fields.js`、`case.html` | Claude | 文件預覽與報告格式：長表格拆分、跨頁表頭與分頁行為 | **進行中**。**併行注意**：另一個 session（「職務設計交接審查」）同時在改授權碼／配額，占用 `functions/index.js`、`functions/lib/quota.js`、`tools/job-design/index.html`、`case.html`、`codes.html`、`jd-ai.js`，且其改動未提交。本工作包**不碰這六個檔案**，`case.html` 僅唯讀。禁止 commit、合併 main、部署。<br>**已完成／寫入權釋放**。實際只改 `jd-word.js`（`jd-theme.css` 未動——文件預覽走 iframe `srcdoc`，帶自己的 `<style>`，與模組主題無關）。**①九欄寬表拆卡**：`anchorTable`／`maturityTable` 原本一列九欄（職能／定義／L1–L5／最低／理想），A4 扣邊界僅約 16.6cm，`table-layout:fixed` 又無 colgroup 故平均分配，每欄約 1.8cm、9.5pt 中文一行不到 5 字；示範資料 L1–L5 中位 17 字、最長 28 字，等於每格折成 4–7 行。改為「索引表（職能／最低／理想）＋一職能一張卡（等級｜可觀察行為兩欄）」，行為描述獨佔約 14cm。**②`problemTable` 補 colgroup**：七欄平均分配使「問題類型」「主／次」浪費寬度，改 20/9/8/15/20/14/14%。**③跨頁**：九張多列表格的表頭包進 `<thead>`（Word 跨頁重複表頭），標題加 `page-break-after:avoid`、`<tr>` 與錨點卡加 `page-break-inside:avoid`，並同步進 `inlineStyles()` 的 `TAG_STYLE`／`CLASS_STYLE`（`inlineStyles` 標籤白名單補 `tr`）。**驗證**：兩個種子案例×四份文件共 8 份重新產出，去標籤後逐段比對——`jd`／`jobdesc` 文字完全一致；`guide`／`selection` 差異僅為錨點表改版（職能名於索引與卡片各出現一次、新增 L1–L5 列標與 ✓ 標記、note 文案加長）。另以實際錨點資料逐項核對，四份文件共 220 段等級行為**零遺失**、✓最低／✓理想標記零遺失。輸出殘留檢查顯示除 `WordSection1` 容器外所有標籤皆已套上 inline style。`node --check`（ESM）通過、`git diff --check` 通過。**未執行**：真實下載後以 Microsoft Word 開啟、實體列印、瀏覽器操作、登入。**併行事故（需使用者知悉）**：另一個 session 於本工作包進行中時將整個工作區 blanket-stage，把 `jd-word.js` 的本次改動一併收進 `bcb8853`／`8898450` 並推上 `main`，因此本變更**已隨 CI 自動部署到 live**，未經預覽與使用者發布核可。 |
| 2026-07-23 | JOBDESIGN-CODES-ADMIN-026 | 寫：`functions/index.js`、`tools/job-design/codes.html`、本占用表 | Claude | 依使用者決策：停權維持「只擋之後兌換」；授權碼將趨近商品／現金流，管理頁需補規則、權責、狀態與誤刪防護 | **已完成／Functions 已部署／Hosting 待部署**。**① 停權語意定案**：維持只擋之後的兌換、不回收已發出次數（使用者 2026-07-23 決策），`consumeQuota` 不變、不新增每次呼叫的 Firestore 讀取。規則改為寫在管理頁上而非靠記憶。**② 新增「已發出」狀態**：新增 `markCodeSent` callable（限 Super Admin）與 `jd_codes.sent`／`sentAt`。理由：原本「未使用」同時涵蓋「還在我手上」與「已交付但對方未兌換」，後者被誤刪等於把已交付的商品收回，且對方要到兌換時才會發現。**③ 誤刪防護**：`deleteCode` 對 `sent === true` 的碼要求帶 `confirmSent`，前端改為 `prompt` 要求**完整輸入授權碼**才刪；未標記已發出者維持一般 `confirm`。刪除日誌加記 `wasSent` 與 `cohort`。**④ 狀態顯示修正**：原為 `revoked ? 已停權 : redeemedBy ? 已兌換 : 未使用` 的三選一，導致「已停權且已兌換」只顯示已停權，管理者看不出底下還有人在用；改為可並存的多標籤。**⑤ 管理頁補規則區塊**：誰能操作、停權的真實效力、刪除的限制與其技術理由、已發出標記的用途、加值只加一次。**⑥ 統計摘要**：總數／未發出／已發出待兌換／已兌換／已停權／已發出總次數（講師碼不計入）。**⑦ 稽核欄位**：`listCodes` 補回 `sent`／`sentAt`／`redeemedAt`，新增 `tsToIso()` 統一序列化 Timestamp；表格顯示產生與兌換日期。**⑧ 順帶修正兩個既有缺陷**：(a) `issueCodes` 解構預設仍為 `quota = 30`，導致前一次改的 `Number(quota) || 60` 在呼叫端未帶 quota 時完全不生效，已改為 60；(b) 新增 `assertSuperAdmin()` 收斂重複的權限檢查（本次僅套用於 `deleteCode`、`markCodeSent`；`issueCodes`／`listCodes`／`setCodeRevoked` 三處**尚未收斂**，仍為各自複製的版本）。**驗證**：`functions/index.js` 與 `codes.html` inline module `node --check` 通過；以六種組合（未發出／已發出／已兌換／停權且已兌換／停權未兌換／講師碼）驗證狀態標籤不再互相覆蓋、統計數字正確、講師碼不計入已發出次數；`firebase deploy --only functions` 成功，`markCodeSent` 建立、其餘八個更新成功。**未執行**：真實登入下的標記／刪除端到端測試、Hosting 部署（本則撰寫時）。**未做**：停權回收既有額度（依使用者決策不做）；`issueCodes`／`listCodes`／`setCodeRevoked` 的權限檢查收斂。 |
| 2026-07-23 | 完成 JOBDESIGN-CODES-ADMIN-026：授權碼管理頁補上規則權責說明、「已發出」狀態、打字確認刪除、可並存的狀態標籤與統計摘要；順帶修好 issueCodes 配額預設仍為 30 的缺陷。 |
| 2026-07-23 | JOBDESIGN-AUTHZ-REFACTOR-027 | 寫：`functions/index.js`、本占用表 | Claude（職務設計交接審查 session） | 收斂重複的 Super Admin 權限檢查 | **已完成／Functions 已部署**。五個管理用 callable（`issueCodes`／`listCodes`／`setCodeRevoked`／`deleteCode`／`markCodeSent`）原本各自複製一份「未登入檢查 → 取 db → 讀 counselors → 驗 isSuperAdmin 與 isActive」，共六份（含 `assertSuperAdmin` 本身），任一處寫錯即為權限漏洞。全部改用 `assertSuperAdmin(req, action)`，淨減 19 行。**行為零變動**：以 `git show HEAD` 逐字比對改動前後的錯誤訊息，`只有管理者可以{產生／查看／停權／刪除／標記}授權碼。` 完全相同；未登入仍回 `unauthenticated`／`請先登入。`；`isSuperAdmin !== true` 的實作僅剩 1 處（即共用函式）；匯出仍為 9 個。`firebase deploy --only functions` 九個全部更新成功。**未執行**：真實登入下的權限端到端測試。 |
| 2026-07-23 | 完成 JOBDESIGN-AUTHZ-REFACTOR-027：五個管理用 callable 的 Super Admin 檢查收斂為單一 assertSuperAdmin，錯誤訊息逐字不變。 |
| 2026-07-23 | JOBDESIGN-CASES-CLOUD-028 | 寫：`tools/job-design/jd-store.js`、`tools/job-design/index.html`、`case.html`、`tests/rules.test.mjs`、本占用表；唯讀：`firestore.rules` | Claude | 案例資料上雲（交接文件標「開課前必須完成」，原本只存 localStorage，換裝置即遺失） | **已完成／Hosting 待部署／Rules 早已在正式環境**。**做法**：不把 `jd-store.js` 的同步介面改成 async（所有呼叫端都是 `const c=S.getCase(id)` 之後一路同步操作，改 async 等於重寫兩頁的每個呼叫點）。改為 localStorage 續作**同步工作副本**、Firestore 為同步／備份層：新增 `initCloud()`（進頁面先把雲端案例合併進本機）、`resyncCloud()`（登入／登出後重跑）、`cloudStatus()`；`createCase`／`saveCase`／`deleteCase` 標記 dirty 並延遲 `setDoc`，刪除同時刪雲端。**合併策略**：以案例為單位 last-write-wins 比 `updated_at`（單人多裝置依序使用，非多人協作，為刻意取捨）。`case.html` 用 module script 的 top-level `await S.initCloud()` 後才讀案例，故所有同步介面不動；`index.html` 的同步統一交給 `onAuth`（載入即觸發一次），避免與獨立 `initCloud()` 並發交錯覆蓋合併結果。**未登入或雲端失敗一律降級為純本機**，不擋畫面。**Firestore 結構**：`jd_cases/{caseId}` = `{ownerUid, updatedAt, payload:整份案例}`；`firestore.rules` 的 `jd_cases`（僅 `ownerUid===uid` 可讀寫、建立不得掛他人名下）**早於本工作包就已存在並部署**，本次未改規則。**驗證**：`npm run test:rules` 43 項全過，含**新增 7 項 `jd_cases` 隔離測試**（本人讀寫建刪、他人讀改刪、冒名建立、未登入全部正確允許／拒絕）；合併邏輯以 5 種裝置情境模擬（換新裝置拉雲端、本機較新保留並回推、雲端較新覆蓋、離線新建上傳、各自獨立聯集）全對；靜態站瀏覽器實測：頁面無 console error、`initCloud()` 未登入回 `false` 無例外、本機建立／`saveField`＋`saveCase` 落地／刪除全部正常、頁面不被同步卡住。**未執行**：**真實 Google 登入下的雲端往返與跨裝置實測**（需使用者帳號，代理不代做）；Hosting 部署。**待使用者驗**：登入後在 A 裝置建案例、B 裝置登入同帳號是否看得到。 |
| 2026-07-23 | 完成 JOBDESIGN-CASES-CLOUD-028：職務設計案例上雲（Firestore `jd_cases`），localStorage 續作工作副本、雲端為同步備份層，未登入優雅降級；新增 7 項 Rules 隔離測試，43 項全過。 |
