# 複製成 functions/.env 後填寫。.env 不進版控（全域 .gitignore 擋住）。
#
# ⚠️ 部署前務必確認 functions/.env 存在。
#    Cloud Functions 在部署當下讀取這個檔案；檔案不存在時 AI_ALLOWLIST 會是空的，
#    jdAi 會拒絕所有呼叫（刻意的安全預設，不是壞掉）。
#
# API Key 不放這裡。它在 Secret Manager：
#    npx firebase functions:secrets:set AI_API_KEY

# 配額上線前的臨時允許清單。只有這些 email 可以呼叫 jdAi。
# 留空 = 完全不開放。第三步完成後移除此項。
AI_ALLOWLIST=you@example.com

# 供應商與模型。留空走預設（openai / gpt-4o）。
AI_PROVIDER=openai
AI_MODEL=
